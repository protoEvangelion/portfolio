---
navigationLabel: home
---

import { mix } from "@theme-ui/color"
import { PostCard } from "../components/PostCard"

<Alert variant="success">Actively releasing new blog content 👀</Alert>
<Divider />

# [Ryan Garant](#ryan-garant)

Front End React UI Developer / Senior Engineer based in Silicon Valley / Made in USA 🇺🇸

JavaScript, Functional Programming, React, Gatsby.js, Styled Components, Jest, Cypress, GraphQL, Node.js, TypeScript


<Divider />

## [intro](#intro)

Hey there! I like JavaScript. I like helping people. Combine the two & you get a blog that delves deep into the trenches of bleeding edge tech, productivity hacks, & time honored principles inspired by Functional Programming! Enjoy 😊

<Divider />

<Flex
  sx={{ flexWrap: 'wrap'}}
>
<Box
  sx={{
    width: ["100%", "100%", "55%"],
    mb: 3,
    pr: [0,0, 4],
  }}
>

## contact

<Text>Twitter</Text>
<Link
  href="https://twitter.com/protoEvangelion"
  target="_blank"
  variant="styles.a"
>
  https://twitter.com/protoEvangelion
</Link>
<Divider />

<Text>GitHub</Text>
<Link
  href="https://github.com/protoEvangelion"
  target="_blank"
  variant="styles.a"
>
  https://github.com/protoEvangelion
</Link>
<Divider />

<Text>Web</Text>
<Link
  href="https://www.pauliescanlon.io"
  target="_blank"
  variant="styles.a"
>
  https://rhino.codes/
</Link>
<Divider />
<Divider />
</Box>

<Box
  sx={{
    width: ["100%", "100%", "45%"],
    mb: 3
  }}
>

## blog topics

<Divider />

<SourceTags>
  {(tags) => (
    <Box>
      {tags
        .sort((a, b) => b.percent - a.percent)
        .map((tag, index) => {
          const { name, count, percent } = tag
          return (
            <Badge
              key={index}
              variant="primary"
              sx={{
                mr: 2,
                mb: 2,
                color: mix("secondary", "primary", `${index / tags.length}`),
                borderColor: mix(
                  "secondary",
                  "primary",
                  `${index / tags.length}`
                ),
              }}
            >
              {name}
            </Badge>
          )
        })}
    </Box>
  )}
</SourceTags>

<Divider />
<Divider />

</Box>
</Flex>

## latest posts

<Divider />

<SourceList filter="posts">
  {(posts) => (
    <Flex
      sx={{
        flexWrap: "wrap",
        ml: (theme) => `-${theme.space[2]}px`,
        mr: (theme) => `-${theme.space[2]}px`,
      }}
    >
      {posts
        .filter((edge) => !edge.node.frontmatter.isPrivate)
        .sort(
          (a, b) =>
            new Date(b.node.frontmatter.date) -
            new Date(a.node.frontmatter.date)
        )
        .splice(0, 4)
        .map((edge, index) => {
          const {
            frontmatter: { title, featuredImage, tags, date },
            excerpt,
            fields: { slug },
          } = edge.node
          return (
            <PostCard
              key={index}
              title={title}
              featuredImage={featuredImage}
              tags={tags}
              date={date}
              excerpt={excerpt}
              slug={slug}
            />
          )
        })}
    </Flex>
  )}
</SourceList>
<Divider />
<Flex
  sx={{
    justifyContent: "center",
  }}
>
  <Box>
    <Button as="a" variant="styles.buttons.ghost" href="/posts">
      More posts ✏️
    </Button>
  </Box>
</Flex>